
import 'package:flutter/material.dart';

Widget textPrice(String text) {
  return Text(
    text,
    style: const TextStyle(
      fontSize: 18,
      color: Color.fromRGBO(60, 60, 60, 1.0),
    ),
  );
}
